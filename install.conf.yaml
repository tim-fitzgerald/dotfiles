- defaults:
    link:
      relink: true

- shell:
  - sudo chsh -s $(which zsh) $(whoami)

- link:
    ~/.zshrc:
      force: true
    ~/.tmux.conf:
      force: true
    ~/.vimrc:
      force: true
    ~/.aliases:
      force: true
    ~/.functions:
      force: true